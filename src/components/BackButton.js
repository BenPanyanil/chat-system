import React from "react";

export default function BackButton() {
  return (
    <div
      style={{
        background: "#ededed",
        padding: "5px 10px",
        border: "1px solid",
        borderRadius: "3px",
        display: "inline-block",
      }}
    >
      Back
    </div>
  );
}
